/*
 * This is the interface for the Envelope model.
 * id: A number representing the unique identifier for the envelope.
 * name: A string representing the name of the envelope.
 * amount: A number representing the amount of money in the envelope.
 */
export interface Envelope {
    id: number;
    name: string;
    amount: number;
}